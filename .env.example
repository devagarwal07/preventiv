# MongoDB Connection (use MongoDB Atlas or local)
MONGODB_URI=mongodb://localhost:27017/prevntiv

# JWT Secret (change this to a random secure string in production)
JWT_SECRET=prevntiv-jwt-secret-change-in-production

# Server Port
PORT=5000
